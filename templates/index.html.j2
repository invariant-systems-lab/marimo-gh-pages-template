<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Interactive Notebooks | Erik Goughnour</title>
  <meta name="description" content="Interactive marimo notebooks and applications for signal processing, numerical methods, and data visualization.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style>
    :root {
      --deep-navy: #0f172a;
      --card-bg: #1e293b;
      --soft-slate: #94a3b8;
      --muted-slate: #64748b;
      --warm-accent: #6366f1;
      --light-accent: #a5b4fc;
      --teal-accent: #14b8a6;
      --subtle-gold: #fbbf24;
      --off-white: #f8fafc;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background-color: var(--deep-navy);
      color: var(--off-white);
      line-height: 1.6;
      min-height: 100vh;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 2rem;
    }

    header {
      border-bottom: 1px solid var(--card-bg);
      padding-bottom: 2rem;
      margin-bottom: 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .header-left {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .logo {
      height: 32px;
      width: auto;
      opacity: 0.9;
    }

    .site-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--off-white);
    }

    .back-link {
      color: var(--soft-slate);
      text-decoration: none;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.875rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      transition: color 0.2s ease;
    }

    .back-link:hover {
      color: var(--off-white);
    }

    .back-link svg {
      width: 16px;
      height: 16px;
    }

    .section-header {
      margin-bottom: 1.5rem;
    }

    .section-header h2 {
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--muted-slate);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-bottom: 0.5rem;
    }

    .section-header p {
      color: var(--soft-slate);
      font-size: 0.95rem;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .card {
      background-color: var(--card-bg);
      border-radius: 12px;
      overflow: hidden;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      text-decoration: none;
      color: inherit;
      display: block;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 16px 32px rgba(0, 0, 0, 0.25);
    }

    .card-header {
      padding: 1.25rem 1.5rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .card-header h3 {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--off-white);
      margin: 0;
    }

    .card-header.notebook h3::before {
      content: "";
      display: inline-block;
      width: 10px;
      height: 10px;
      background-color: var(--teal-accent);
      border-radius: 50%;
      margin-right: 0.75rem;
    }

    .card-header.app h3::before {
      content: "";
      display: inline-block;
      width: 10px;
      height: 10px;
      background-color: var(--warm-accent);
      border-radius: 50%;
      margin-right: 0.75rem;
    }

    .card-body {
      padding: 1.25rem 1.5rem;
    }

    .card-body p {
      color: var(--soft-slate);
      font-size: 0.875rem;
      margin-bottom: 1rem;
    }

    .card-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.8rem;
      color: var(--teal-accent);
      text-decoration: none;
      transition: color 0.2s ease;
    }

    .card-link:hover {
      color: var(--off-white);
    }

    .card-link.app {
      color: var(--warm-accent);
    }

    .card-link svg {
      width: 14px;
      height: 14px;
    }

    footer {
      border-top: 1px solid var(--card-bg);
      padding-top: 2rem;
      text-align: center;
    }

    .footer-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    .footer-links {
      display: flex;
      gap: 2rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .footer-links a {
      color: var(--soft-slate);
      text-decoration: none;
      font-size: 0.875rem;
      transition: color 0.2s ease;
    }

    .footer-links a:hover {
      color: var(--off-white);
    }

    .footer-note {
      color: var(--muted-slate);
      font-size: 0.75rem;
      font-family: 'JetBrains Mono', monospace;
    }

    @media (max-width: 640px) {
      .container {
        padding: 1.5rem;
      }

      header {
        flex-direction: column;
        align-items: flex-start;
      }

      .cards {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="header-left">
        <img src="https://raw.githubusercontent.com/marimo-team/marimo/main/docs/_static/marimo-logotype-thick.svg" alt="marimo" class="logo">
        <span class="site-title">Interactive Notebooks</span>
      </div>
      <div style="display: flex; gap: 1.5rem; align-items: center;">
        <a href="https://erikgoughnour.com" class="back-link">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="19" y1="12" x2="5" y2="12"></line>
            <polyline points="12 19 5 12 12 5"></polyline>
          </svg>
          Portfolio
        </a>
        <a href="./" class="back-link" style="color: var(--teal-accent);">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="3" width="7" height="7"></rect>
            <rect x="14" y="3" width="7" height="7"></rect>
            <rect x="14" y="14" width="7" height="7"></rect>
            <rect x="3" y="14" width="7" height="7"></rect>
          </svg>
          All Notebooks
        </a>
      </div>
    </header>

    <main>
      {% if notebooks %}
      <section class="section-header">
        <h2>Notebooks</h2>
        <p>Interactive notebooks with editable code - explore and experiment</p>
      </section>
      <div class="cards">
        {% for notebook in notebooks %}
        <a href="{{ notebook.html_path }}" class="card">
          <div class="card-header notebook">
            <h3>{{ notebook.display_name }}</h3>
          </div>
          <div class="card-body">
            <p>Open in edit mode to view and modify the code</p>
            <span class="card-link">
              Open Notebook
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <polyline points="12 5 19 12 12 19"></polyline>
              </svg>
            </span>
          </div>
        </a>
        {% endfor %}
      </div>
      {% endif %}

      {% if apps %}
      <section class="section-header">
        <h2>Applications</h2>
        <p>Interactive apps with a clean interface - code hidden for focused exploration</p>
      </section>
      <div class="cards">
        {% for app in apps %}
        <a href="{{ app.html_path }}" class="card">
          <div class="card-header app">
            <h3>{{ app.display_name }}</h3>
          </div>
          <div class="card-body">
            <p>Run in app mode with hidden code</p>
            <span class="card-link app">
              Launch App
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <polyline points="12 5 19 12 12 19"></polyline>
              </svg>
            </span>
          </div>
        </a>
        {% endfor %}
      </div>
      {% endif %}
    </main>

    <footer>
      <div class="footer-content">
        <div class="footer-links">
          <a href="https://erikgoughnour.com">Portfolio</a>
          <a href="https://github.com/egoughnour" target="_blank" rel="noopener">GitHub</a>
          <a href="https://marimo.io" target="_blank" rel="noopener">marimo.io</a>
          <a href="https://docs.marimo.io" target="_blank" rel="noopener">Documentation</a>
        </div>
        <p class="footer-note">Built with marimo WebAssembly</p>
      </div>
    </footer>
  </div>
</body>
</html>
